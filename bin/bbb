#!/bin/sh

if [ ! "$BUILDDIR" ]; then
  echo "Couldn't locate the top of the 9615."
  exit 1
fi

if [ -z "$1" ] ; then
 bitbake -c cleanall linux-quic && bitbake linux-quic
else
 bitbake -c cleanall $@ && bitbake $@
fi
